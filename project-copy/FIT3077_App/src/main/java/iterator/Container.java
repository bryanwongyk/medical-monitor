package iterator;

/**
 * A container interface for an aggregate structure for the iterator design pattern.
 * @author Bryan
 */
public interface Container {
    public ContainerIterator getIterator();
}
